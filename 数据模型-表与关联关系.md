# 实验室设备数据采集子模块 — 数据库表与关联关系

> 用于功能开发 codex/AI 学习的数据模型参考文档

---

## 一、核心表概览

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `ib_tbs_detailedinf` | 详情记录主表（附件/文件头） | fdiseq (PK), ffilenm, fcontent (BLOB), fhiino, fdatno, fspno |
| `ib_tbs_tbldat` | 文件与详情的关联桥表 | fdiseq, fpkseq (fguid), fpkseq2, fcol, ftblnm |
| `lis_instdata_new` | 解析后的仪器检测数据（新模式） | fdiseq, finstno, sampno, itemseq, rslt, rslt1-rslt6 |
| `lis_instdata` | 解析后的仪器检测数据（旧模式） | fdiseq, instno, sampno, itemseq, rslt, testdt |
| `lis_sample` | 样品主数据 | sampseq (PK), sampno |
| `lp_tbc_instchkitem` | 仪器检测项目配置表 | instno, instchkitem, itemsysseq, itemname, fws |
| `ib_tbs_instfilegetlog` | 仪器文件采集日志 | flogid, fdiseq, ffilenm, ffiledt, fgetdt, fhiino |

---

## 二、完整数据流（两种上传路径）

### 路径 A：HTTP 上传（新模式）

```
┌──────────────────────────────────────────────────────────────┐
│                     HTTP 文件上传请求                         │
│  参数: instno, fguid, filename, hiiNo, empId, data...       │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  UploadInstDataFilesNew.insidePerform()                      │
│                                                              │
│  ① saveIbDetailedInf(session)                                │
│     └─ INSERT IBDetailedInf → 生成 diseq (主键)              │
│     └─ INSERT ib_tbs_tbldat (ftblnm='INSTFILE', fpkseq=fguid)│
│     └─ Common.saveFileInDetailTbl() → 文件存入 BLOB          │
│                                                              │
│  ② dealInstData(session)                                     │
│     └─ Factory.getInstance(instno) → new AgilentLC()         │
│     └─ dao.getInstData(session, values, filePath, sessionInfo)│
│        └─ PDF解析 → JSONArray                                │
│        └─ saveData() → INSERT lis_instdata_new               │
│                                                              │
│  ③ commit / rollback                                        │
└─────────────────────────────────────────────────────────────┘
```

### 路径 B：文件监控（旧模式）

```
┌──────────────────────────────────────────────────────────────┐
│  InstDataMonitor (定时任务，每30秒执行)                       │
│                                                              │
│  扫描目录: {appPath}/ftpFiles/upload/                        │
│  文件命名规则: INSTRUMENT-CODE_YYYYMMDDHHMMSS.扩展名           │
│  例: AGILENT-6890N_20150324124324.TXT                        │
│                                                              │
│  ① addDetailedInf() → INSERT IBDetailedInf → 生成 diseq      │
│  ② addDetailedInfInst() → INSERT ib_tbs_wtfiledinf_inst      │
│  ③ Factory.getInstance(instDataFile) → 获取解析类             │
│  ④ dao.getInstData(conn, input, diseq, instno, filepath)     │
│     └─ 旧模式 → Item 对象 → SaveInstData2Db → lis_instdata   │
│  ⑤ CommonHiis.saveFileInIbDetailTbl() → 文件入 BLOB          │
│  ⑥ 移动文件到 processed/ 目录                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、单表详细结构

### 3.1 `ib_tbs_detailedinf` — 详情/文件头表

```
表名: ib_tbs_detailedinf
同义词: IBDetailedInf
视图: IB_vs_DetailedInf
主键: fdiseq (SEQUENCE: seq_DetailedInf_diseq)
```

| 字段 | 类型 | 说明 |
|------|------|------|
| fdiseq | NUMBER(18) PK | 详情记录序号，贯穿整个流程的核心ID |
| fbiseq | NUMBER(16) | 业务序号，FK: IL_tbs_spbaseinf.biseq |
| forgno | NUMBER(12) | 机构代码 |
| fspno | VARCHAR2(6) | 专业代码 |
| fdatno | VARCHAR2(12) | 资料代码，仪器上传固定为 '0' 或 '00' |
| finfseq | NUMBER(6) | 序号，同个业务有多个附件时的序号 |
| fdetailedinf | XMLTYPE | 详细内容 (XML格式) |
| fmodiXSL | VARCHAR2(6) | 修改模板 |
| fbrowXSL | VARCHAR2(6) | 浏览模板 |
| fprintXSL | VARCHAR2(6) | 打印模板 |
| ffilenm | VARCHAR2(120) | 文件名 |
| fcontent | BLOB | 文件二进制内容 |
| fprog | VARCHAR2(20) | 对应程序 |
| fvaliddt | DATE | 有效时间 |
| finvaliddt | DATE | 停用时间 |
| fsignseq | NUMBER(16) | 签章序号 |
| fopdt | DATE | 操作时间 |
| fempID | VARCHAR2(12) | 操作人 |
| fhiino | NUMBER(6) | 机构编号（数据隔离） |
| flocparanm | VARCHAR2(50) | 存储位置参数 |
| frealfilenm | VARCHAR2(100) | 真实文件名 |
| fpath | VARCHAR2(400) | 上传前本地路径 |
| fifuploadfile | VARCHAR2(8) | 是否上传文件 |
| ftemplseq | NUMBER | 模板序号 |

**数据隔离**: 通过 `fhiino` 区分不同机构的数据

---

### 3.2 `ib_tbs_tbldat` — 表数据关联桥表

```
表名: ib_tbs_tbldat
用途: 建立"业务对象(fguid)"与"详情记录(diseq)"之间的关联
```

| 字段 | 类型 | 说明 |
|------|------|------|
| ftblnm | VARCHAR2 | 关联的表名，仪器文件固定为 'INSTFILE' |
| fpkseq | VARCHAR2 | 主键值1，仪器文件传 fguid |
| fpkseq2 | VARCHAR2 | 主键值2，固定为 'F' |
| fcol | VARCHAR2 | 列名，固定为 'ALL' |
| fdiseq | NUMBER | 关联的详情记录序号 |
| fopdt | DATE | 操作时间 |

**关联关系**:
```
ib_tbs_tbldat.fpkseq = 上传请求的 fguid
ib_tbs_tbldat.fdiseq = ib_tbs_detailedinf.fdiseq
ib_tbs_tbldat.fdiseq = lis_instdata_new.fdiseq
```

---

### 3.3 `lis_instdata_new` — 仪器检测数据（新模式）

```
表名: htlis.lis_instdata_new
用途: 存储解析后的仪器检测项目结果
数据隔离: fhiino
```

| 字段 | 类型 | 说明 |
|------|------|------|
| fguid | VARCHAR2(32) | 记录GUID |
| finstno | VARCHAR2(40) | 仪器编码，如 'AGILENT-LC' |
| sampseq | NUMBER | 样品序号（关联 lis_sample） |
| sampno | VARCHAR2(80) | 样品编号 |
| itemseq | VARCHAR2(80) | 检测项目编号 |
| rslt | VARCHAR2(200) | 检测结果（主结果值） |
| fempid | VARCHAR2(12) | 操作人 |
| fopdt | DATE | 操作时间 |
| fhiino | NUMBER(6) | 机构编号 |
| fdiseq | NUMBER | 关联详情记录 |
| rslt1 | VARCHAR2 | 扩展结果1（保留时间 RT） |
| rslt2 | VARCHAR2 | 扩展结果2（峰类型 MM/BB） |
| rslt3 | VARCHAR2 | 扩展结果3（峰面积） |
| rslt4 | VARCHAR2 | 扩展结果4（含量/峰面积比） |
| rslt5 | VARCHAR2 | 扩展结果5（含量） |
| rslt6 | VARCHAR2 | 扩展结果6（组分名称） |

**AgilentLC 写入映射**:
```
rslt  = 峰面积 (rslt3)    ← 作为主结果
rslt1 = 保留时间
rslt2 = 峰类型 (MM/BB)
rslt3 = 峰面积
rslt4 = 含量/峰面积比
rslt5 = 含量 [ug/mL]
rslt6 = 组分名称 (飞燕草色素、矢车菊色素...)
```

**保存逻辑**:
```
① DELETE WHERE sampno=? AND itemseq=? AND fhiino=?    -- 删除旧数据
② INSERT ...                                           -- 批量插入新数据
③ UPDATE sampseq FROM lis_sample WHERE sampno=t.sampno  -- 关联样品
```

---

### 3.4 `lis_instdata` — 仪器检测数据（旧模式）

```
表名: lis_instdata
用途: 旧模式存储解析后的检测数据
区别: 字段命名略有不同（无 f 前缀），无扩展字段 rslt1-rslt6
```

| 字段 | 类型 | 说明 |
|------|------|------|
| fseq | NUMBER(12) PK | 序列号 |
| instno | VARCHAR2(40) | 仪器编码 |
| testdt | DATE | 检测时间 |
| sampno | VARCHAR2(80) | 样品编号 |
| testno | VARCHAR2(80) | 检测编号 |
| itemseq | VARCHAR2(80) | 检测项目编号 |
| itemnm | VARCHAR2(200) | 检测项目名称 |
| rslt | VARCHAR2(200) | 检测结果 |
| rsltdesc | VARCHAR2(200) | 结果描述 |
| mw | VARCHAR2(80) | 计量单位 |
| insertdt | DATE | 插入时间 |
| remark | VARCHAR2(128) | 备注 |
| ifok | VARCHAR2(8) | 是否合格 |
| fdiseq | NUMBER | 关联详情记录 |
| conc | VARCHAR2 | 浓度 |
| resultseq | NUMBER | 结果序号 |
| equaval | VARCHAR2(80) | 通道 |
| x1value~x10value | VARCHAR2(80) | 标准曲线 x 值 |
| y1value~y10value | VARCHAR2(80) | 标准曲线 y 值 |

**使用路径**: `Item对象 → SaveInstData2Db.savaObject() → lis_instdata`

---

### 3.5 `lis_sample` — 样品主数据

```
表名: htlis.lis_sample
用途: 样品基础信息
```

| 字段 | 类型 | 说明 |
|------|------|------|
| sampseq | NUMBER PK | 样品序号 |
| sampno | VARCHAR2(80) | 样品编号（业务主键） |
| ... | | 其他样品属性字段 |

**与解析数据的关联**:
```sql
-- lis_instdata_new 通过此更新关联样品序号
UPDATE lis_instdata_new t 
SET sampseq = NVL((SELECT sampseq FROM lis_sample WHERE sampno = t.sampno), 0)
WHERE fdiseq = ?;
```

---

### 3.6 `lp_tbc_instchkitem` — 仪器检测项目配置

```
表名: htlis.lp_tbc_instchkitem
用途: 配置每个仪器型号有哪些检测项目，以及项目名称到编号的映射
```

| 字段 | 类型 | 说明 |
|------|------|------|
| instno | VARCHAR2(40) | 仪器编码，如 'AGILENT-LC' |
| instchkitem | VARCHAR2 | 仪器端检测项目标识（如 '飞燕草色素'） |
| instchkitemnm | VARCHAR2 | 检测项目显示名 |
| itemsysseq | VARCHAR2 | 系统检测项目编号（关联 lis_item） |
| itemname | VARCHAR2 | 检测项目名称 |
| fws | VARCHAR2 | 精度（小数位数） |

**作用**: 解析时，将 PDF 中读到的组分名称（如 '飞燕草色素'）映射为系统项目编号（itemsysseq）

**AGILENT-LC 示例数据**:
```sql
INSERT INTO htlis.lp_tbc_instchkitem(instno, instchkitem, instchkitemnm, itemsysseq, itemname, fws)
VALUES ('AGILENT-LC', '飞燕草色素', '飞燕草色素', '1001', '飞燕草色素', '5');
-- 矢车菊色素、矮牵牛色素、芍药色素、天竺葵色素、锦葵色素 同理
```

---

### 3.7 `ib_tbs_instfilegetlog` — 仪器文件采集日志

```
表名: ib_tbs_instfilegetlog
用途: 记录文件采集过程的日志
```

| 字段 | 类型 | 说明 |
|------|------|------|
| flogid | VARCHAR2(32) PK | 日志ID (sys_guid) |
| fdiseq | NUMBER | 关联详情记录 |
| ffilenmnew | VARCHAR2 | 新文件名（上传后的命名） |
| ffilenm | VARCHAR2 | 原始文件名 |
| fpath | VARCHAR2 | 文件原始路径 |
| ffiledt | DATE | 文件时间 |
| fgetdt | DATE | 采集时间 |
| fhiino | NUMBER(6) | 机构编号 |

---

## 四、表关联关系总图

```
                        ┌─────────────────┐
                        │   HTTP 上传请求   │
                        │  fguid = 'xxx'   │
                        └────────┬────────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
                    ▼            ▼            ▼
          ┌──────────────┐ ┌──────────┐ ┌──────────────────┐
          │ib_tbs_tbldat │ │IBDetailed│ │ lis_instdata_new │
          │              │ │   Inf    │ │                  │
          │ftblnm=       │ │fdiseq PK │ │fdiseq ──────────┤
          │ 'INSTFILE'   │ │ffilenm   │ │finstno='AGILENT- │
          │fpkseq=fguid──┤→│fcontent  │ │  LC'             │
          │fpkseq2='F'   │ │  (BLOB)  │ │sampno            │
          │fdiseq ───────┤→│fhiino    │ │itemseq ──────────┤
          └──────────────┘ │fopdt     │ │rslt, rslt1-6     │
                           │fempId    │ │fhiino            │
                           └──────────┘ └───────┬──────────┘
                                                 │
                    ┌────────────────────────────┘
                    │ UPDATE sampseq
                    ▼
          ┌──────────────────┐     ┌──────────────────────┐
          │   lis_sample     │     │ lp_tbc_instchkitem   │
          │ sampseq PK       │     │ instno='AGILENT-LC'  │
          │ sampno ──────────┤────→│ instchkitem=         │
          │ ...              │     │   '飞燕草色素'        │
          └──────────────────┘     │ itemsysseq='1001'    │
                                   └──────────────────────┘
```

---

## 五、常用查询 SQL

### 查询某个上传的文件列表
```sql
SELECT d.fdiseq, d.ffilenm, d.fopdt, d.fempId,
       b.fpkseq AS fguid, b.ftblnm
FROM ib_tbs_detailedinf d
JOIN ib_tbs_tbldat b ON b.fdiseq = d.fdiseq AND b.ftblnm = 'INSTFILE'
WHERE d.fhiino = ? AND d.fdatno = '0'
ORDER BY d.fopdt DESC;
```

### 查询某个文件的解析数据
```sql
SELECT t.sampno, t.itemseq, t.rslt,
       t.rslt1 AS 保留时间, t.rslt2 AS 峰类型,
       t.rslt3 AS 峰面积, t.rslt5 AS 含量,
       t.rslt6 AS 组分名称
FROM lis_instdata_new t
WHERE t.fdiseq = ?
ORDER BY t.rslt1;
```

### 查询某样品所有的历史检测数据
```sql
SELECT d.ffilenm, d.fopdt AS 上传时间,
       t.finstno, t.itemseq, t.rslt, t.rslt6
FROM lis_instdata_new t
JOIN ib_tbs_detailedinf d ON d.fdiseq = t.fdiseq
WHERE t.sampno = ?
ORDER BY d.fopdt DESC;
```

### 查询某个仪器已配置的检测项目
```sql
SELECT instchkitem, itemsysseq, itemname, fws
FROM lp_tbc_instchkitem
WHERE UPPER(instno) = UPPER('AGILENT-LC');
```

---

## 六、开发文档页面时的数据映射

| 功能页面 | hf | 涉及表 | 主要操作 |
|---------|-----|--------|---------|
| 采集文档列表 | syssjcj_cjwd_list | ib_tbs_detailedinf + ib_tbs_tbldat | SELECT 查询 |
| 文档详情 | syssjcj_cjwd_detail | ib_tbs_detailedinf | SELECT fdiseq 查文件 |
| 解析数据列表 | syssjcj_cjwd_data_list | lis_instdata_new + lis_sample | SELECT 按 fdiseq |
| 重新解析 | syssjcj_cjwd_reparse_form | lis_instdata_new | DELETE + 重新调解析 |
| 脚本解析信息 | syssjcj_cjwd_script_detail | 待建表 | 待实现 |
| 图谱查询 | syssjcj_cjwd_spectrum_list | 原始文件 BLOB | SELECT fcontent |
| 原图查看 | syssjcj_cjwd_image_view | ib_tbs_detailedinf.fcontent | SELECT BLOB 展示 |
