# 采集数据日志与采集配置管理——表结构规划

> 文档状态：开发基线（规划稿）  
> 更新时间：2026-08-10  
> 数据库：Oracle，业务 Schema 为 `HTLIS`  
> 适用页面：采集数据日志、采集配置管理  
> 注意：本文用于固定设计和指导后续 DDL/接口开发，不是可直接执行的建表脚本。正式执行前必须在 PL/SQL 中核对现有表、字段、约束、索引和存量数据。

## 1. 规划边界

本阶段只升级 HTTP 采集策略，覆盖：

- 客户端注册和当前运行状态；
- 客户端心跳历史；
- 文件发现、上传成功、上传失败等采集业务日志；
- HTTP 采集策略的查看、修改、发布、启用、停用和下载；
- 客户端拉取策略及应用结果回执；
- 平台人工维护日志和客户端自动应用日志；
- “我的采集策略”。

本阶段不开放：

- USB、串口策略的远程编辑；
- 脚本存储、在线调试和远程执行；
- 项目对照表、原始记录数据源与客户端配置文件的混合下发。

项目对照表和数据源管理仍可保留在“采集配置管理”页面中，但作为独立业务页签和独立数据表开发，不进入本次客户端 HTTP 策略链路。

## 2. 操作主体和平台固定字段

### 2.1 平台人工操作

平台页面从父窗口取得当前用户信息：

```javascript
var s = window.parent.reEcdtJ.sInfo;

var userName = s.empNm;              // 用户姓名，如“监*一”
var empId = s.empId || s.opNo;       // 当前系统中 opNo 与 empId 相同，如 S11
var hiiNo = s.hiiNo;                 // 当前机构，如 5300003
```

人工新增、修改、发布、启用、停用策略时，固定字段写入规则：

| 字段 | 写入值 | 示例 |
|---|---|---|
| `FEMPID` | 当前平台操作人 `empId/opNo` | `S11` |
| `FOPDT` | 数据库或服务端时间 | `SYSDATE` |
| `FHIINO` | 当前平台机构 `hiiNo` | `5300003` |

注册 SQL 使用：

```text
empid_sql_equal
empnm_sql_equal（页面或日志确需姓名时）
hiino_sql_equal
```

`USER NAME` 不是本模块统一固定字段。页面显示姓名时：

1. 当前操作页面优先使用 `reEcdtJ.sInfo.empNm`；
2. 历史列表根据 `FEMPID` 关联平台操作员/人员表取得姓名；
3. 若后续审计要求保存不可变姓名，再在审计表增加姓名快照字段，不修改固定字段语义。

### 2.2 客户端自动操作

心跳、采集日志、策略拉取和策略应用回执不是人工操作，不写入 `S11`：

| 字段 | 写入规则 |
|---|---|
| `FEMPID` | 已注册的 `CLIENT_ID`，如 `AGILENT-1200` |
| `FOPDT` | Java 服务端实际接收/入库时间 |
| `FHIINO` | Java 根据 `CLIENT_ID` 从客户端注册表取得，禁止直接信任 HTTP 报文中的机构值 |

策略日志通过 `FOPERATORTYPE` 区分：

```text
USER    平台人工操作
CLIENT  客户端自动操作
SYSTEM  服务端自动任务
```

## 3. 总体表关系

```text
HTLIS.LIS_CLIENT_INFO
    ├── HTLIS.LIS_HEARTBEAT_LOG
    ├── HTLIS.LIS_ACQUISITION_LOG
    └── HTLIS.LIS_CLIENT_POLICY
            └── HTLIS.LIS_CLIENT_POLICY_LOG
```

| 表 | 当前状态 | 作用 |
|---|---|---|
| `LIS_CLIENT_INFO` | 已有，增量升级 | 客户端注册信息和最新状态 |
| `LIS_HEARTBEAT_LOG` | 已有，增量升级 | 每次心跳的历史快照 |
| `LIS_ACQUISITION_LOG` | 已有预留表，增量升级并启用 | 文件发现、上传和配置应用等业务日志 |
| `LIS_CLIENT_POLICY` | 计划新建 | 每个客户端当前策略及发布版本 |
| `LIS_CLIENT_POLICY_LOG` | 计划新建 | 策略变更、发布、拉取和应用审计日志 |

## 4. HTLIS.LIS_CLIENT_INFO

### 4.1 业务定位

- 一台已注册客户端一条记录；
- `CLIENT_ID` 为稳定唯一标识；
- 保存注册归属、软件版本和最新运行状态；
- 客户端未知或未启用时，Java 接口拒绝心跳和策略拉取；
- `LAB_ID` 仅作为实验室显示/业务编码，权限和数据隔离以 `FHIINO` 为准。

### 4.2 现有字段保留

| 字段 | 类型 | 说明 |
|---|---|---|
| `CLIENT_ID` | `VARCHAR2(100)` | 主键，客户端唯一编号 |
| `CLIENT_TYPE` | `VARCHAR2(20)` | `python`、`go` |
| `CLIENT_VER` | `VARCHAR2(40)` | 客户端程序版本 |
| `LAB_ID` | `VARCHAR2(50)` | 实验室业务编码，仅供显示/分组 |
| `INSTNO` | `VARCHAR2(100)` | 仪器/解析接口编号 |
| `OS_INFO` | `VARCHAR2(200)` | 操作系统信息 |
| `INSTALL_TIME` | `DATE` | 首次登记/首次上线时间 |
| `LAST_HEARTBEAT` | `DATE` | 最近心跳时间 |
| `CURRENT_STATUS` | `VARCHAR2(20)` | 当前状态 |
| `RUNNING_MODE` | `VARCHAR2(30)` | 当前运行模式，本阶段为 `http` |
| `UPLOAD_TOTAL` | `NUMBER` | 客户端累计上传成功数 |
| `UPLOAD_FAIL` | `NUMBER` | 客户端累计上传失败数 |
| `UPTIME_SEC` | `NUMBER` | 本次启动累计运行秒数 |

### 4.3 计划增加字段

| 字段 | 建议类型 | 必填 | 说明 |
|---|---|---:|---|
| `FEMPID` | `VARCHAR2(100)` | 是 | 最后自动更新主体，心跳更新时为 `CLIENT_ID` |
| `FOPDT` | `DATE DEFAULT SYSDATE` | 是 | 最后更新时间 |
| `FHIINO` | `NUMBER(12)` | 是 | 服务端确认的数据所属机构 |
| `FREGEMPID` | `VARCHAR2(60)` | 是 | 在平台登记该客户端的人员编号 |
| `FREGDT` | `DATE` | 是 | 平台登记时间 |
| `FENABLE` | `VARCHAR2(10)` | 是 | `是/否`，是否允许客户端访问接口 |
| `FAPPLIEDVER` | `VARCHAR2(40)` | 否 | 客户端已应用的策略版本 |
| `FAPPLYSTATUS` | `VARCHAR2(20)` | 否 | `NONE/PENDING/SUCCESS/FAILED` |
| `FAPPLYMSG` | `VARCHAR2(2000)` | 否 | 最近一次策略应用结果 |
| `FLASTCONFIGDT` | `DATE` | 否 | 最近策略拉取或应用时间 |
| `FAUTHKEYID` | `VARCHAR2(60)` | 否 | 客户端凭证标识，不是密钥本身 |
| `FAUTHSECRETENC` | `VARCHAR2(1000)` | 否 | 服务端加密保存的客户端密钥，不允许页面回显 |
| `FAUTHSTATUS` | `VARCHAR2(20)` | 否 | `ACTIVE/DISABLED/EXPIRED` |

### 4.4 约束与索引规划

```text
PK  (CLIENT_ID)
IDX (FHIINO, FENABLE)
IDX (FHIINO, INSTNO)
IDX (FHIINO, LAST_HEARTBEAT)
IDX (CURRENT_STATUS, LAST_HEARTBEAT)
```

### 4.5 写入规则

- 客户端必须先由平台登记，未知 `CLIENT_ID` 不再由首个心跳自动注册；
- 登记时：`FREGEMPID=S11`、`FHIINO=5300003`；
- 心跳时：`FEMPID=CLIENT_ID`、`FOPDT=服务端时间`；
- 心跳报文中的 `INSTNO/LAB_ID` 只能用于核对，不得覆盖服务端注册归属；
- 当前在线状态由 `LAST_HEARTBEAT` 与服务端阈值计算，不只信任 `CURRENT_STATUS`。

## 5. HTLIS.LIS_HEARTBEAT_LOG

### 5.1 业务定位

每次心跳一条不可变记录，用于在线判断、重启识别、状态追踪和运行详情。

### 5.2 现有字段保留

```text
SEQ_ID
CLIENT_ID
HEARTBEAT_SEQ
HEARTBEAT_TIME
STATUS
RUNNING_MODE
UPLOAD_TOTAL
UPLOAD_FAIL
UPTIME_SEC
ERROR_MSG
```

### 5.3 计划增加字段

| 字段 | 建议类型 | 说明 |
|---|---|---|
| `FGUID` | `VARCHAR2(60)` | 稳定日志标识，唯一约束 |
| `FEMPID` | `VARCHAR2(100)` | 自动事件写 `CLIENT_ID` |
| `FOPDT` | `DATE DEFAULT SYSDATE` | 服务端实际入库时间 |
| `FHIINO` | `NUMBER(12)` | 服务端从客户端注册信息取得 |
| `INSTNO` | `VARCHAR2(100)` | 仪器编号快照 |
| `CLIENT_VER` | `VARCHAR2(40)` | 客户端版本快照 |

### 5.4 索引规划

```text
UK  (FGUID)
IDX (FHIINO, FOPDT)
IDX (CLIENT_ID, FOPDT)
IDX (CLIENT_ID, HEARTBEAT_SEQ)
```

`HEARTBEAT_TIME` 保留为心跳业务时间；当前由服务端生成时可与 `FOPDT` 相同。

## 6. HTLIS.LIS_ACQUISITION_LOG

### 6.1 业务定位

保存客户端真实产生的采集业务事件。当前客户端尚未完整上报该表所需日志，必须在 Python、Go 和 Java 协议统一后启用。

### 6.2 现有字段保留

```text
SEQ_ID
CLIENT_ID
LOG_TIME
LOG_LEVEL
LOG_TYPE
INSTNO
FILE_NAME
FILE_SIZE
MESSAGE
RAW_DETAIL
```

### 6.3 计划增加字段

| 字段 | 建议类型 | 说明 |
|---|---|---|
| `FGUID` | `VARCHAR2(60)` | 日志唯一标识 |
| `FEMPID` | `VARCHAR2(100)` | 自动事件写 `CLIENT_ID` |
| `FOPDT` | `DATE DEFAULT SYSDATE` | 服务端入库时间 |
| `FHIINO` | `NUMBER(12)` | 服务端确认的机构 |
| `FDISEQ` | `NUMBER(20)` | 上传成功后关联归档文件 |
| `RESULT_STATUS` | `VARCHAR2(20)` | `SUCCESS/FAILED` |
| `HTTP_STATUS` | `NUMBER(5)` | HTTP 返回状态码 |
| `DURATION_MS` | `NUMBER(12)` | 本次操作耗时 |
| `RETRY_COUNT` | `NUMBER(6) DEFAULT 0` | 重试次数 |
| `ERROR_CODE` | `VARCHAR2(100)` | 标准错误编码 |

### 6.4 日志类型字典

仅使用客户端真实会产生的类型：

```text
CLIENT_START
CLIENT_STOP
FILE_FOUND
UPLOAD_SUCCESS
UPLOAD_FAIL
CONFIG_PULL
CONFIG_APPLY_SUCCESS
CONFIG_APPLY_FAIL
```

### 6.5 索引规划

```text
UK  (FGUID)
IDX (FHIINO, FOPDT)
IDX (CLIENT_ID, FOPDT)
IDX (FHIINO, LOG_TYPE, FOPDT)
IDX (FDISEQ)
IDX (RESULT_STATUS, FOPDT)
```

## 7. HTLIS.LIS_CLIENT_POLICY（计划新建）

### 7.1 业务定位

- 保存客户端当前策略；
- 一个客户端最多一条当前策略；
- 草稿保存不会影响客户端；
- 发布后生成新的 `CONFIG_VER`，客户端才可拉取；
- “我的采集策略”按 `FOWNEREMPID` 查询，不能按最后修改人查询。

### 7.2 字段规划

| 字段 | 建议类型 | 必填 | 说明 |
|---|---|---:|---|
| `FGUID` | `VARCHAR2(60)` | 是 | 主键 |
| `CLIENT_ID` | `VARCHAR2(100)` | 是 | 对应 `LIS_CLIENT_INFO.CLIENT_ID`，唯一 |
| `INSTNO` | `VARCHAR2(100)` | 是 | 仪器编号 |
| `FOWNEREMPID` | `VARCHAR2(60)` | 是 | 策略创建人/归属人 |
| `CONFIG_VER` | `VARCHAR2(40)` | 是 | 如 `v2_20260810153000` |
| `POLICY_STATUS` | `VARCHAR2(20)` | 是 | `DRAFT/PUBLISHED/DISABLED` |
| `INTERFACE_TYPE` | `VARCHAR2(20)` | 是 | 本阶段固定 `http` |
| `FILE_PATH` | `VARCHAR2(1000)` | 是 | 客户端监听目录 |
| `SCAN_INTERVAL` | `NUMBER(10)` | 是 | 扫描间隔秒数 |
| `SERVICE_URL` | `VARCHAR2(500)` | 是 | 服务地址，发布时校验白名单 |
| `START_ROW` | `NUMBER(10)` | 否 | 解析起始行 |
| `SAMP_COL_FLAG` | `VARCHAR2(100)` | 否 | 样品列标记 |
| `TRACK_MODE` | `NUMBER(2)` | 是 | 去重跟踪模式 |
| `ALLOWED_EXTENSIONS` | `VARCHAR2(500)` | 是 | 允许扩展名 |
| `MAX_COMPANION_FILES` | `NUMBER(10)` | 否 | 配套文件上限 |
| `HEARTBEAT_INTERVAL` | `NUMBER(10)` | 是 | 心跳间隔秒数 |
| `FPUBLISHEDDT` | `DATE` | 否 | 最近发布时间 |
| `FEMPID` | `VARCHAR2(60)` | 是 | 最后平台操作人，如 `S11` |
| `FOPDT` | `DATE DEFAULT SYSDATE` | 是 | 最后操作时间 |
| `FHIINO` | `NUMBER(12)` | 是 | 当前机构 |

### 7.3 约束与索引规划

```text
PK  (FGUID)
UK  (CLIENT_ID)
IDX (FHIINO, POLICY_STATUS)
IDX (FHIINO, FOWNEREMPID)
IDX (FHIINO, FOPDT)
```

### 7.4 版本规则

```text
v1_yyyyMMddHHmmss
v2_yyyyMMddHHmmss
```

- 修改草稿不改变已发布版本；
- 每次发布版本号递增并带发布时间戳；
- 客户端比较 `CONFIG_VER` 与 `FAPPLIEDVER` 判断是否需要更新。

## 8. HTLIS.LIS_CLIENT_POLICY_LOG（计划新建）

### 8.1 业务定位

保存策略生命周期及客户端应用结果，供“采集数据日志—采集策略维护日志”查询。

### 8.2 字段规划

| 字段 | 建议类型 | 必填 | 说明 |
|---|---|---:|---|
| `FGUID` | `VARCHAR2(60)` | 是 | 主键 |
| `POLICY_GUID` | `VARCHAR2(60)` | 是 | 关联策略 |
| `CLIENT_ID` | `VARCHAR2(100)` | 是 | 目标客户端 |
| `CONFIG_VER` | `VARCHAR2(40)` | 是 | 对应策略版本 |
| `ACTION_TYPE` | `VARCHAR2(30)` | 是 | 操作类型 |
| `FOPERATORTYPE` | `VARCHAR2(20)` | 是 | `USER/CLIENT/SYSTEM` |
| `BEFORE_JSON` | `CLOB` | 否 | 修改前快照 |
| `AFTER_JSON` | `CLOB` | 否 | 修改后快照或下发内容 |
| `RESULT_STATUS` | `VARCHAR2(20)` | 是 | `SUCCESS/FAILED` |
| `RESULT_MESSAGE` | `VARCHAR2(2000)` | 否 | 执行结果/失败原因 |
| `FEMPID` | `VARCHAR2(100)` | 是 | 人工操作为 `S11`，自动操作为 `CLIENT_ID` |
| `FOPDT` | `DATE DEFAULT SYSDATE` | 是 | 操作时间 |
| `FHIINO` | `NUMBER(12)` | 是 | 所属机构 |

### 8.3 操作类型字典

```text
CREATE
MODIFY
PUBLISH
ENABLE
DISABLE
CLIENT_PULL
APPLY_SUCCESS
APPLY_FAIL
```

### 8.4 索引规划

```text
PK  (FGUID)
IDX (FHIINO, FOPDT)
IDX (POLICY_GUID, FOPDT)
IDX (CLIENT_ID, FOPDT)
IDX (ACTION_TYPE, FOPDT)
IDX (FEMPID, FOPDT)
```

## 9. HTTP 接口与表写入关系

### 9.1 UploadClientLog.m

```text
心跳
  → UPDATE LIS_CLIENT_INFO
  → INSERT LIS_HEARTBEAT_LOG

采集业务日志
  → INSERT LIS_ACQUISITION_LOG
```

服务端必须先按 `CLIENT_ID` 查询 `LIS_CLIENT_INFO`，取得可信 `INSTNO/FHIINO/FENABLE`，再写日志。

### 9.2 GetClientConfig.m（计划新增）

```text
客户端提交 CLIENT_ID + 当前版本 + 凭证
  → 校验 LIS_CLIENT_INFO
  → 查询 LIS_CLIENT_POLICY.POLICY_STATUS='PUBLISHED'
  → 有新版本时返回 HTTP 策略
  → 写 LIS_CLIENT_POLICY_LOG(CLIENT_PULL)
```

### 9.3 UploadClientConfigAck.m（计划新增）

```text
客户端提交 CONFIG_VER + 应用结果
  → 更新 LIS_CLIENT_INFO.FAPPLIEDVER/FAPPLYSTATUS/FAPPLYMSG
  → 写 LIS_CLIENT_POLICY_LOG(APPLY_SUCCESS/APPLY_FAIL)
  → 必要时同步写 LIS_ACQUISITION_LOG
```

## 10. 页面与注册 SQL 规划

### 10.1 采集数据日志（050xx）

| 查询号 | 作用 | 主要表 |
|---|---|---|
| `ynjksys_05001q` | 客户端汇总统计 | `LIS_CLIENT_INFO`、`LIS_ACQUISITION_LOG` |
| `ynjksys_05002q` | 客户端列表 | `LIS_CLIENT_INFO` |
| `ynjksys_05003q` | 客户端消息日志 | `LIS_ACQUISITION_LOG` |
| `ynjksys_05004q` | 客户端日志详情 | `LIS_ACQUISITION_LOG`、`LIS_HEARTBEAT_LOG` |
| `ynjksys_05005q` | 策略维护日志 | `LIS_CLIENT_POLICY_LOG` |
| `ynjksys_05006q` | 策略日志详情 | `LIS_CLIENT_POLICY_LOG` |

所有查询必须使用当前 `hiino_sql_equal` 限定 `FHIINO`。

### 10.2 采集配置管理（060xx）

| 查询号 | 作用 | 类型 |
|---|---|---|
| `ynjksys_06001q` | 采集策略列表 | 查询 |
| `ynjksys_06002q` | 采集策略详情 | 查询 |
| `ynjksys_06003q` | 采集策略保存 | 写入 |
| `ynjksys_06004q` | 策略发布/启停 | 写入 |
| `ynjksys_06005q` | 策略下载 | 查询/导出 |
| `ynjksys_06006q` | 客户端选项 | 查询 |

`06003q/06004q` 必须写入：

```text
FEMPID = empid_sql_equal
FOPDT  = SYSDATE
FHIINO = hiino_sql_equal
```

页面通过 `reEcdtJ.sInfo.empNm` 显示当前用户名；数据库权限、机构和操作人仍需在注册 SQL 中校验。

## 11. 页面内容调整基线

### 11.1 采集数据日志

保留页签：

- 客户端消息日志；
- 采集策略维护日志。

保留统计：

- 客户端数量；
- 在线客户端；
- 上传成功；
- 上传失败。

筛选：日志时间、仪器设备、客户端、日志级别、消息类型、执行结果。机构由登录会话限定，不提供可编辑机构筛选。

趋势图代码继续保留注释，不恢复渲染。

### 11.2 采集配置管理

策略列表展示：客户端、仪器、监听目录、扫描间隔、文件类型、策略版本、客户端已应用版本、应用状态、策略状态、最后修改人、最后修改时间。

操作：查看、下载、修改、发布、启用/停用。

“我的采集策略”按：

```sql
FOWNEREMPID = 当前 empid_sql_equal
```

脚本相关代码继续注释隐藏。

## 12. 升级与实施顺序

1. 在 PL/SQL 核对三张现有表的真实字段、约束、索引和数据量；
2. 生成只增不删的升级 DDL，不直接删除或重建现有表；
3. 为现有客户端补登记 `CLIENT_ID/INSTNO/FHIINO/FREGEMPID`；
4. 升级 `UploadClientLog.java`，禁止未知客户端自动注册和伪造机构；
5. 增加 `LIS_CLIENT_POLICY/LIS_CLIENT_POLICY_LOG`；
6. 开发 `GetClientConfig.m/UploadClientConfigAck.m`；
7. 对齐 Python、Go 客户端 HTTP 协议；
8. 注册 `050xx/060xx` SQL；
9. 页面从 `mockMode: true` 切换为真实服务；
10. 使用真实客户端完成心跳、日志、策略发布、拉取、应用、失败回滚全链路验证。

## 13. 当前事实与未完成项

当前已经存在：

- `LIS_CLIENT_INFO`、`LIS_HEARTBEAT_LOG`、`LIS_ACQUISITION_LOG` 初版DDL；
- `UploadClientLog.java` 心跳 UPSERT/INSERT 代码；
- Python、Go 客户端心跳配置项；
- 两个页面的模拟数据和服务切换占位。

当前尚未完成：

- 现有三张表的平台固定字段升级；
- 可信的 `CLIENT_ID → INSTNO → FHIINO` 注册映射；
- 客户端凭证校验；
- 客户端详细日志上报；
- 策略主表与策略日志表；
- 策略拉取与应用回执接口；
- Python、Go 客户端远程配置应用；
- `050xx/060xx` 注册 SQL；
- 页面真实数据切换。

在上述前提完成前，页面不得把模拟在线状态或模拟日志当作真实客户端运行结果。
