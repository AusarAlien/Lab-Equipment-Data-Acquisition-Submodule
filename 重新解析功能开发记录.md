# 采集文档重新解析功能开发记录

## 一、功能边界

“重新解析”位于“采集文档和数据管理 → 解析数据”工作页内，不增加新的菜单入口或独立跳转页面。

用户输入重新解析原因并确认后：

1. 页面显示阻塞遮罩，禁止关闭、删除、下载及重复提交等其他操作。
2. 服务端从采集文件归档中恢复原文件及伴随文件。
3. 服务端按文件对应的 `FINSTNO` 调用原仪器解析器。
4. 原解析器执行旧数据删除和新数据写入。
5. 服务端验证新的解析数据已经入库，并记录重新解析审计结果。
6. 页面在当前弹窗内重新查询解析数据和解析信息，不打开新窗口。
7. 成功时自动提示“重新解析完成”；失败时解除遮罩并保留原解析数据。

数据库持久状态只使用“成功”和“失败”。执行过程仅通过页面遮罩表达，不写入“解析中”状态。

## 二、已实现内容

### 1. 数据库

- `ynjksys_02010d-采集文件重新解析支撑表.sql`
  - `HTLIS.LIS_INSTFILE_PART`：保存文件归档的仪器身份和伴随文件 BLOB。
  - `HTLIS.LIS_INST_REPARSE_LOG`：保存原因、执行人、机构、执行时间、前后数据量及成功/失败信息。
- `ynjksys_02011q-采集文件最近重新解析信息查询.sql`
  - 查询指定文件最近一次重新解析审计信息。

原始主文件仍使用 `HII.IB_TBS_DETAILEDINF.FCONTENT`，不重复保存主文件 BLOB。

### 2. Java 服务端

- `InstFileArchiveSupport.java`
  - 上传成功后保存 `FDISEQ`、`FINSTNO`、主文件名和伴随文件。
- `InstFileReparse.java`
  - 提供 `/yncdc/InstFileReparse.m`。
  - 校验登录会话、机构范围、文件标识和重新解析原因。
  - 将 BLOB 写入私有临时目录，按 `Factory.getInstance(instno)` 调用现有解析器。
  - 使用 JDBC 保存点保护旧解析结果；解析失败或新数据为 0 时回滚到重新解析前状态。
  - 写入成功或失败审计记录，并清理临时文件。
- `UploadInstDataFilesNew.java`
  - 上传归档完成后调用 `InstFileArchiveSupport.archive(...)` 保存重新解析所需上下文。

### 3. 前端

- `syssjcj_cjwd_service.js`
  - 增加重新解析接口请求和最近审计信息查询。
- `syssjcj_cjwd_parse_workspace.js`
  - 实际提交重新解析原因。
  - 执行期间显示阻塞遮罩。
  - 成功后在当前工作页重新加载解析数据与解析信息并弹出完成提示。
  - 接口失败时显示原因，解除遮罩，不伪造成功状态。

## 三、历史数据兼容边界

1. 已成功解析的单文件历史数据，可从 `LIS_INSTDATA_NEW` 反查 `FINSTNO`，因此 AGILENT-1200、BRUKER-MICROFLEX 等单文件接口可重新解析。
2. 从未成功解析且尚未写入新归档元数据的历史文件，无法可靠判断应调用哪个仪器解析器，系统应明确提示“历史文件缺少仪器身份”，不能猜测接口。
3. AXIOIMAGERZ2 必须同时使用 PDF 和 TXT。旧上传链路只保存主文件 BLOB，历史记录若未归档 TXT，系统应拒绝重新解析并提示缺少伴随文件，不能在缺失坐标的情况下静默覆盖旧数据。
4. 新表和新版上传接口启用后产生的 AXIOIMAGERZ2 文件，可保存伴随 TXT，具备完整重新解析条件。

## 四、部署顺序

1. 在数据库执行 `ynjksys_02010d` 建表脚本。
2. 在平台注册查询 SQL `ynjksys_02011q`。
3. 打包并部署 `InstFileArchiveSupport.java`、`InstFileReparse.java` 和修改后的 `UploadInstDataFilesNew.java`。
4. 将两个前端 JS 平铺更新到服务器 `/data/conda_envs/isauto/ispyfr/static/ynjksys`。
5. 清理浏览器缓存后进行联调。

不要先部署新版上传接口再建支撑表，否则新文件上传完成后会在归档补充步骤失败。

## 五、验收用例

1. 单文件成功重新解析：数据量更新、状态成功、审计原因和操作人正确。
2. 解析器报错：页面提示失败，旧解析数据仍然存在，失败审计已记录。
3. 解析结果为 0：按失败处理并恢复旧数据。
4. 同一文件重复点击：第一次执行期间页面不可再次操作。
5. 跨机构 `FDISEQ`：服务端拒绝访问。
6. 原因为空或少于 2 字：前后端均拒绝提交。
7. 新上传 AXIOIMAGERZ2 PDF+TXT：能恢复两份文件并完整重新解析。
8. 历史 AXIOIMAGERZ2 缺少 TXT：明确提示缺少伴随文件，不覆盖旧数据。
9. 历史失败文件缺少 `FINSTNO`：明确提示缺少仪器身份。

## 六、当前未执行事项

- 尚未在实际数据库执行建表脚本或注册查询 SQL。
- 尚未将 Java 类打包上传服务器。
- 尚未进行真实数据库端到端重新解析测试。
- SVN 中既有 `Agilent1200.java` 存在未解决的冲突标记，整包构建前需单独处理；本次未修改该文件。
